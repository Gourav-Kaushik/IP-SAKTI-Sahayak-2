# Environment Configuration for IP-SAKTI Sahayak

# Set Node environment (recommended for production)
NODE_ENV=production

# (Optional) Port - Render automatically sets this at runtime (default: 3000)
# PORT=10000

# (Optional) RAG Hybrid Legal Search Configuration (All have default fallbacks)
# HYBRID_FUSION_STRATEGY=weighted
# TFIDF_WEIGHT=0.5
# SEMANTIC_WEIGHT=0.5
# HYBRID_GUARDRAIL_THRESHOLD=0.22

